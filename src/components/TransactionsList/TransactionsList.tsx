import { Box } from "theme-ui";

export function TransactionsList() {
  return (
    <Box
      sx={{
        maxWidth: 740,
        px: 4,
        mx: "auto",
        bg: "gray.dark",
        border: "1px solid",
        borderColor: "gray.medium",
        borderRadius: 8,
      }}
    >
      transactions
    </Box>
  );
}
